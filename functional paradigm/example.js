const add = function(flock_x, flock_y) {
    return flock_x + flock_y
}

const multiply = function(flock_x, flock_y) {
    return flock_x * flock_y
}

const flock_a = 4
const flock_b = 2
const flock_c = 0

const result = add(multiply(flock_b, add(flock_a, flock_c)), multiply(block_a, flock_b))